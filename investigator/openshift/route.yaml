apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: investigator
  namespace: investigator
  labels:
    app: investigator
  annotations:
    # Sticky sessions for SignalR (Blazor Server)
    haproxy.router.openshift.io/balance: source
    haproxy.router.openshift.io/disable_cookies: "false"
spec:
  host: investigator.apps.core.ci.devcluster.openshift.com
  to:
    kind: Service
    name: investigator
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
